/**
 * The FoundryVTT module id (also used as the namespace for game.settings).
 *
 * Keep this value in ONE place to avoid typos across imports.
 * If the module id ever changes, changing it here updates the whole codebase.
 */
export const MODULE_ID = "rusbar-homerules-for-wod-v20-system";
